version: "3.9"

x-build: &dockerfile-dev-build
  context: .
  dockerfile: Dockerfile.dev

services:
  app:
    build: *dockerfile-dev-build
    env_file: .env
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: [
      "scripts/start-dev.sh"
    ]
